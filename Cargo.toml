[workspace]
members = [
    "programs/*"
]
resolver = "2"

[profile.release]
opt-level = 3
overflow-checks = true
lto = "fat"
codegen-units = 1
panic = "abort"
[profile.release.build-override]
incremental = false
codegen-units = 1 

 
